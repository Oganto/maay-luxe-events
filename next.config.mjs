/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Add remote domains here if you host images/video off Vercel (e.g. a CMS or CDN)
    remotePatterns: [],
  },
};

export default nextConfig;
